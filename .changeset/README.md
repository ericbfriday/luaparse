# Changesets

This folder contains changeset files used by [@changesets/cli][].

When making changes that affect published packages, add a changeset by running:

```sh
npm run changeset
```

Changeset files are consumed during `npm run version` and removed after version bumps are committed.

[@changesets/cli]: https://github.com/changesets/changesets
